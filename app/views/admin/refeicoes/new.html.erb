<% content_for :title, "Nova refeição | Admin" %>

<% actions = capture do %>
  <%= link_to admin_refeicoes_path, class: "btn btn-outline-light" do %>
    <i class="bi bi-arrow-left-circle me-2"></i>Voltar
  <% end %>
<% end %>

<%= render layout: "shared/admin_shell", locals: {
  title: "Nova refeição",
  subtitle: "Cadastre uma refeição para controle por QR Code.",
  breadcrumbs: [
    { label: "Home", path: admin_root_path },
    { label: "Refeições", path: admin_refeicoes_path },
    { label: "Nova refeição" }
  ],
  actions: actions
} do %>
  <div class="card border-0 rounded-4 shadow-sm">
    <div class="card-body p-4">
      <%= render "form" %>
    </div>
  </div>
<% end %>
