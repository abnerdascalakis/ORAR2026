<% content_for :title, "Recuperar Senha | ORAR 2026" %>

<%= render "devise/shared/auth_card",
           eyebrow: "Recuperacao",
           title: "Esqueceu sua senha?",
           description: "Informe seu e-mail para receber as instrucoes de redefinicao." do %>
  <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "d-grid gap-3" }) do |f| %>
    <%= f.error_notification %>

    <%= f.input :email,
                label: "E-mail",
                required: true,
                autofocus: true,
                input_html: { autocomplete: "email", class: "orar-auth-input px-3" },
                label_html: { class: "fw-semibold text-dark" },
                wrapper_html: { class: "mb-0" } %>

    <%= f.submit "Enviar instrucoes", class: "btn orar-auth-button fw-semibold" %>
  <% end %>

  <div class="orar-auth-links mt-4 pt-3 border-top">
    <%= render "devise/shared/links" %>
  </div>
<% end %>
